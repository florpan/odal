# Normalise line endings to LF in the repo and on checkout, so Prettier and
# .editorconfig agree with git on every platform.
* text=auto eol=lf
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.woff2 binary
