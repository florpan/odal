export function HelpBar() {
  return (
    <div className="help">
      LMB select / drag box · RMB command · A attack-move · Ctrl+1–9 groups · arrows / middle-drag pan · wheel zoom ·
      hotkeys build · S stop · Esc cancel
    </div>
  );
}
